import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devcled85.planocortevidracaria',
  appName: 'Plano de Corte - Vidraçaria',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
